using Core.Enums;

namespace Web2PnK.Models
{
    public static class UserRoles
    {
        public const string Admin = nameof(AspNetRoles.Admin);
        public const string User = nameof(AspNetRoles.User);
    }
}
